# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Electron packaging output (regenerated by `npm run dist` etc.) — large
# binaries that don't belong in git history.
desktop/build-unpacked
desktop/dist-final
desktop/dist-test
desktop/dist-release
desktop/prepackaged-final

# PHP portable + copie buildée de l'API, régénérées par
# `scripts/bundle-php.cjs` / `build-installer.cjs` avant chaque build — des
# binaires et du vendor Composer n'ont rien à faire dans git.
desktop/resources/php
desktop/resources/api
desktop/.test-instance

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
